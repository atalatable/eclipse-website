const config = {
    database: {
        connectionLimit: 10,
        host: "localhost",
        user: "root",
        password: "",
        database: "eclipse",
        dateString: true
    }
}

module.exports = config;